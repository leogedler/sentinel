FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

# Source is mounted at runtime via volume — no COPY or build step needed.
# --host exposes Vite's dev server to all interfaces so Docker can reach it.

EXPOSE 8080

CMD ["npm", "run", "dev", "--", "--host"]
