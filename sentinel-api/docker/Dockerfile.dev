FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

# Source is mounted at runtime via volume — no COPY or build step needed.
# CMD is inherited per-service (api vs slack-bot) through docker-compose.dev.yml.

EXPOSE 3000

CMD ["npm", "run", "dev"]
