FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY tsconfig.json ./
COPY src ./src

RUN npm run build

# Slack bot runs as a long-lived Socket Mode process
# No port needed — uses outbound WebSocket connection to Slack
CMD ["node", "dist/index.js"]
